I want to implement an automatic image watermark system in my Node.js + Express backend.

Requirements:

1. When a user uploads an image (using multer) during ad post  , the backend should automatically add a watermark before saving or returning the image.

2. Use the "sharp" library for image processing.

3. Watermark details:
   - Use a PNG logo stored at: /uploads/watermark.png
   - Position: bottom-right (southeast)
   - Resize watermark to a reasonable width (e.g., 120–150px)
   - Keep transparency (do not fully block the image)

4. Flow:
   - User uploads image
   - Image is temporarily stored
   - Apply watermark
   - Save final watermarked image
   - Return only watermarked image URL
   - Delete original (non-watermarked) file

5. Code structure:
   - Create a reusable utility function: addWatermark(inputPath, outputPath)
   - Integrate into existing upload route

6. Bonus features:
   - Option to change position (center, top-left, etc.)
   - Option to dynamically add text watermark (like website name)
   - Handle multiple image uploads

7. Important:
   - Optimize performance (no blocking)
   - Handle errors properly
   - Ensure images are compressed (optional)

8. Tech stack:
   - Node.js
   - Express
   - Multer
   - Sharp

Provide complete working code including:
- watermark utility file
- updated upload route
- example folder structure